<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.MVDao">
    <select id="recommend" resultType="MV">
        select * from mv
    </select>

    <select id="findById" resultType="MV">
        select * from mv where id=#{id}
    </select>
</mapper>
